# User Configuration

This directory contains your personal configuration files.

## Getting Started

Copy the example files and customize them:
```bash
cp parsers.json.example parsers.json
```

## Files

- `parsers.json` - Your custom parsers (overrides defaults)

## Default Configurations

Default configurations are stored in `src/expense_tracker/config/defaults/`.
These are used when no user config is present.